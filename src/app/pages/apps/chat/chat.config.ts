import { ComponentConfig } from '../../../models';

export const ChatConfig = {
    formConfig: [
        {
            email: {
                type: 'text',
                label: 'Username',
                placeholder: '',
                validators: []
            }
        }
    ]
} as ComponentConfig;
